# dependencies
node_modules/

# next.js
.next/
out/

# env — never commit API keys
.env
.env.local
.env*.local

# user-uploaded chat images (served locally at /uploads, never committed)
public/uploads/

# misc
.DS_Store
*.pem
npm-debug.log*
.gstack/
.vercel

# user projects — filesystem store written by /api/projects, never committed
data/
tsconfig.tsbuildinfo
